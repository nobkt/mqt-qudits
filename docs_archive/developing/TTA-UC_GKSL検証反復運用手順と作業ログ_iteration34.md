# TTA-UC GKSL 検証反復運用手順と作業ログ — Iteration 34

## 日時
2026-03-01

## 目的
Iteration 33 の検証結果分析で発見された **Cell 27 未修正問題** と **検証スクリプトの根本的欠陥** を修正する。

### 前提条件（Iteration 33 で確認済み）
1. **全シミュレーターが正しく動作** — 問題なし
2. **収束特性が正しい** — O(dt) 収束は Stinespring dilation の数学的性質
3. **密度行列品質** — 全 n_steps で機械精度
4. **チェック 1-5 は正しく動作** — シミュレーション結果のチェックは正常

### Iteration 33 で発見された問題（4 件）

1. **Cell 27 精度ガイダンステーブルが依然として未修正**（深刻度: 高）
   - iteration 32, 33 の作業ログでは「修正済み ✅」と記載されているが、
     実際のノートブック ipynb ファイルには反映されていなかった
   - T 列の値が t_max=10.0 のデータのまま（~3e-04, ~8e-05, ~3e-05, ~2e-05）
   - 正しい値（t_max=100.0）: ~1.3e-02, ~4e-03, ~1.5e-03, ~7e-04
   - Cell 26 の実行出力と 37〜51 倍の乖離

2. **Cell 27 の誤った記述「T < 2×10⁻⁵」が未修正**（深刻度: 高）
   - t_max=100.0 の場合、T = 7.32e-04 であり、2×10⁻⁵ の約 37 倍

3. **Cell 27 に n_steps=200 の行が欠落**（深刻度: 中）
   - Cell 26 は n_steps=200 まで計算するが、テーブルに含まれていなかった

4. **検証スクリプトの Cell 27 整合性チェックの根本的欠陥**（深刻度: 高）
   - check_cell27_table_consistency() がハードコード値と実測値を比較するだけ
   - ノートブックの実際の内容を読み取っていないため、修正の検証が不可能
   - 結果的に「PASS」と報告されるが、ノートブックは未修正のまま

## 作業内容

### ステップ 1: 分析レポート作成 ✅
- `developing/検証結果分析_iteration33_結果分析.md` を作成
- 4 つの問題点を詳細に文書化
- 検証スクリプトの欠陥を根本原因まで分析

### ステップ 2: Cell 27 精度ガイダンステーブルの修正 ✅
- `tutorials/quantum_dynamics_gksl_comparison.ipynb` の Cell 27 を直接修正
- T 列の値を t_max=100.0 の実測値（Cell 26 出力）に更新
- n_steps=200 の行を追加
- 精度レベルの表記を実測値に合わせて修正
- `t_max=100.0` の条件をタイトルに明示
- t_max 依存性の説明を新規追加

### ステップ 3: Cell 27 の誤った記述の修正 ✅
- 「T < 2×10⁻⁵」→「T < 1×10⁻³（t_max=100.0 の場合）」に修正

### ステップ 4: 検証スクリプトの根本的改善 ✅
- `tutorials/run_tta_uc_gksl_verification_iteration34.py` を新規作成
- ノートブック ipynb ファイルを JSON として読み込み、Cell 27 の Markdown をパース
- テーブル値を正規表現で抽出し、実測値と比較
- 以下の追加チェックを実装:
  - 全 n_steps 行の存在確認
  - 誤った「T < 2×10⁻⁵」記述の不在確認
  - t_max 条件の記載確認

### ステップ 5: ユーザーが検証スクリプトを実行 ⬜
```bash
cd tutorials
python run_tta_uc_gksl_verification_iteration34.py
```
結果は `developing/verification_results/iteration34_*.json/md` に出力される。

### ステップ 6: 結果分析 ⬜
- iteration 34 の結果を確認し、全チェックが PASS であることを検証

## 期待される結果

### 全チェック PASS
1. トレース距離単調減少: PASS（変更なし）
2. 収束次数 ≈ 1.0: PASS（変更なし）
3. 忠実度単調増加: PASS（変更なし）
4. 密度行列品質: PASS（変更なし）
5. n_steps=100 で T < 1e-3: PASS（変更なし）
6. Cell 27 ノートブック内容検証: PASS（ノートブックを実際に読み取って検証）

## 修正の正当性

### Cell 27 修正内容の根拠
- Cell 26 の実際の出力（t_max=100.0, Hilbert空間次元=81）に合わせた値
- 同一ノートブック内でコードの出力と文書の値が一致することは基本要件
- 修正後の値は計算再現性がある
- n_steps=200 の行を追加し、Cell 26 の全出力をカバー
- t_max 依存性の説明は Stinespring 近似誤差の理論的性質（T ∝ t_max · dt）に基づく

### 検証スクリプト改善の根拠
- ノートブックの実際の内容を検証しない限り、修正の実施を確認できない
- ipynb ファイルは JSON 形式であり、プログラムから直接読み取り可能
- 正規表現によるテーブルパースは安定かつ信頼性が高い

## 参照ファイル
- `developing/検証結果分析_iteration33_結果分析.md` — 分析レポート
- `developing/TTA-UC_GKSL検証反復運用手順と作業ログ_iteration33.md` — 前回の作業ログ
- `tutorials/quantum_dynamics_gksl_comparison.ipynb` — 修正対象（Cell 27）
- `tutorials/run_tta_uc_gksl_verification_iteration34.py` — 改善された検証スクリプト
