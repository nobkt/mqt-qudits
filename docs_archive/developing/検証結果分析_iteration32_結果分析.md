# Iteration 32 検証結果分析

- 分析日時: 2026-03-01
- 分析対象: `developing/verification_results/iteration32_cell27_fix_20260228T232407Z.json` および `.md`
- 分析範囲: コード全体・ノートブック出力・理論・設計・仕様の包括的レビュー

## 1. 検証結果の要約

### 1.1 総合判定: PASS（5 チェック中 5 つ成功）

| チェック項目 | 結果 | 詳細 |
|---|---|---|
| トレース距離単調減少 | ✓ PASS | n_steps 増加に伴い T は単調減少 |
| 収束次数 ≈ 1.0 | ✓ PASS | 平均 Rate(T) = 1.2010（許容範囲内） |
| 忠実度単調増加 | ✓ PASS | n_steps 増加に伴い F は単調増加 |
| 密度行列品質 | ✓ PASS | 全 n_steps で機械精度 |
| n_steps=100 で T < 1e-3 | ✓ PASS | T = 7.32e-04 < 1e-3 |

### 1.2 検証スクリプト自体は正しく動作

iteration32 の検証スクリプト (`run_tta_uc_gksl_verification_iteration32.py`) は正しく動作し、
全チェックが PASS となった。シミュレーターコードにも問題はない。

## 2. 発見された問題点

### 2.1 問題 1: Cell 27 精度ガイダンステーブルが未修正（深刻度: 高）

**iteration 32 の作業ログでは「修正済み ✅」と記載されているが、実際のノートブックファイルでは Cell 27 の内容が修正されていない。**

#### 現在の Cell 27 の記載値（修正されていない）

```markdown
| n_steps | dt | T(ST, exact) | 精度レベル | 推奨用途 |
|--------:|-------:|--------------:|:-----------|:---------|
| 10 | 10.0 | ~3e-04 | 実用十分 | 定性的挙動確認 |
| 20 | 5.0 | ~8e-05 | 高精度 | 人口動態比較 |
| 50 | 2.0 | ~3e-05 | 非常に高精度 | 定量的解析 |
| 100 | 1.0 | ~2e-05 | 極めて高精度 | 高精度比較（本ノートブック使用値） |
```

#### Cell 26 の実測値（t_max=100.0）

| n_steps | dt | T(ST,exact) | Fidelity | Rate(T) |
|--------:|-------:|--------------:|----------:|--------:|
| 10 | 10.0 | 1.294e-02 | 0.99966585 | --- |
| 20 | 5.0 | 4.081e-03 | 0.99996978 | 1.665 |
| 50 | 2.0 | 1.493e-03 | 0.99999631 | 1.098 |
| 100 | 1.0 | 7.319e-04 | 0.99999916 | 1.028 |
| 200 | 0.5 | 3.627e-04 | 0.99999981 | 1.013 |

#### 乖離の詳細

| n_steps | Cell 27 記載 | Cell 26 実測 | 乖離倍率 |
|--------:|:------------|:-------------|:---------|
| 10 | ~3e-04 | 1.294e-02 | **43 倍** |
| 20 | ~8e-05 | 4.081e-03 | **51 倍** |
| 50 | ~3e-05 | 1.493e-03 | **50 倍** |
| 100 | ~2e-05 | 7.319e-04 | **37 倍** |

#### 原因

Cell 27 の T(ST, exact) 列の値は **iteration 30 のデータ（t_max=10.0）** から転記されたもの。
dt 列は t_max=100.0 用の正しい値（10.0, 5.0, 2.0, 1.0）だが、
T 列は t_max=10.0 の結果を使っている。
iteration 32 の作業ログでは修正済みとされているが、実際のノートブックファイルには反映されていない。

### 2.2 問題 2: Cell 27 の誤った記述が未修正（深刻度: 高）

Cell 27 に以下の誤った記述が残っている:

> $O(\Delta t)$ でも `n_steps=100` で `T < 2×10⁻⁵` と十分な精度が得られる

t_max=100.0 の場合、n_steps=100（dt=1.0）では T = 7.32e-04 であり、2×10⁻⁵ の約 37 倍。
この記述は事実と異なる。

### 2.3 問題 3: Cell 27 に n_steps=200 の行が欠落（深刻度: 中）

Cell 26 のコードは n_steps=[10, 20, 50, 100, 200] で計算しており、n_steps=200 の結果も出力されるが、
Cell 27 のガイダンステーブルには n_steps=200 の行が存在しない。

### 2.4 問題 4: iteration31 検証スクリプトの Markdown レポートラベル不整合（深刻度: 低）

`run_tta_uc_gksl_verification_iteration31.py` の 251 行目:

```python
f"| n_steps=100 で T < 5e-5 | {'✓' if practical_accuracy else '✗'} |\n",
```

コード上の閾値チェック（202 行目）は `t100 < 1e-3` に修正済みだが、
Markdown レポート生成部分のラベルが古い閾値 `5e-5` のまま残っている。
結果的に、レポートに記載される閾値と実際の判定基準が不一致になる。

## 3. 問題のないこと（正常動作の確認）

以下の項目には問題は存在しない:

1. **シミュレーターコードに問題なし** — ClassicalGKSLSimulator, QuditGKSLSimulator ともに正しく動作
2. **O(dt) 収束は正しい** — Stinespring dilation の数学的性質として理論的に正しい
3. **密度行列品質は完璧** — トレース保存・エルミート性・正定値性すべて機械精度
4. **Cell 26 のコードは正しい** — 収束解析コード自体は正しく動作し、正しい結果を出力
5. **Cell 25 の理論的背景は正しい** — 3 段階近似の説明は数学的に正確
6. **Stinespring dilation 実装は正しい** — G行列構成、θ=√dt、U=exp(-iθG) すべて正しい
7. **Trotter ステップ実装は正しい** — 対称分割 + 回文順序 Lindblad 積は正しい
8. **iteration32 検証スクリプトは正しい** — 全5チェックが PASS
9. **iteration32 検証結果データ(JSON)は正しい** — 数値データは正確

## 4. 修正方針

### 4.1 Cell 27 精度ガイダンステーブルの修正

Cell 26 の実測値（t_max=100.0）に基づいてテーブルを修正する。
n_steps=200 の行を追加する。

修正後の値:

| n_steps | dt | T(ST, exact) | 精度レベル | 推奨用途 |
|--------:|-------:|--------------:|:-----------|:---------|
| 10 | 10.0 | ~1.3e-02 | 低精度 | 定性的挙動確認 |
| 20 | 5.0 | ~4e-03 | 中精度 | 概略的な比較 |
| 50 | 2.0 | ~1.5e-03 | 高精度 | 人口動態比較 |
| 100 | 1.0 | ~7e-04 | 非常に高精度 | 定量的解析（本ノートブック使用値） |
| 200 | 0.5 | ~4e-04 | 極めて高精度 | 高精度比較 |

### 4.2 Cell 27 の誤った記述の修正

`T < 2×10⁻⁵` の記述を、実測値に基づいた正しい記述に修正する:
- 修正後: `n_steps=100` で `T < 1×10⁻³` と十分な精度が得られる
- t_max 依存性の説明を追加

### 4.3 iteration31 検証スクリプトの Markdown ラベル修正

251 行目のラベルを `T < 5e-5` → `T < 1e-3` に修正する。

### 4.4 iteration33 検証スクリプトの作成

修正後の Cell 27 の内容が正しいことを検証するスクリプトを作成する。
Cell 27 のテーブル値と Cell 26 の実測値の整合性を自動チェックする機能を追加する。

## 5. 修正対象ファイル

1. `tutorials/quantum_dynamics_gksl_comparison.ipynb` — Cell 27 のマークダウン修正
2. `tutorials/run_tta_uc_gksl_verification_iteration31.py` — Markdown レポートラベル修正
3. `tutorials/run_tta_uc_gksl_verification_iteration33.py` — 修正後の検証スクリプト（新規作成）

## 6. 修正後に期待される結果

iteration33 検証スクリプトで全チェックが PASS となる:

1. トレース距離単調減少: PASS（変更なし）
2. 収束次数 ≈ 1.0: PASS（変更なし）
3. 忠実度単調増加: PASS（変更なし）
4. 密度行列品質: PASS（変更なし）
5. n_steps=100 で T < 1e-3: PASS（変更なし）
6. Cell 27 テーブル値と実測値の整合性: PASS（新規チェック）
